import { mkdir, rm } from "fs/promises";

// await mkdir('uploads');
// await mkdir('uploads/images');

// await mkdir("docs/resumes/data", {recursive: true}); it will recursively build all the folders one by one
// await rm("docs/resumes/data", {recursive: true}); it only deletes the data folder
// await rm("docs", {recursive: true}); it removes whole folder and everything inside it